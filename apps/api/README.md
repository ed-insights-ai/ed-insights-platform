# Ed Insights API

FastAPI backend for the Ed Insights Platform.

## Startup

```bash
cd apps/api
uv run uvicorn src.main:app --reload
```

API docs available at [localhost:8000/docs](http://localhost:8000/docs).
